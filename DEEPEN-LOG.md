# DEEPEN-LOG — MiniCSKH

- 2026-09-23 · feat: Chiến dịch gọi ra (Campaign) — port từ SkyCS `Cpn_Campaign_*` (11.BackEnd/V10/idn.SkyCS.Biz/Campaign.cs). Thêm entity `Campaign`/`CampaignCustomer` + enum `CampaignStatus`/`CampaignCustomerStatus`, vòng đời Pending→Approve→Started⇄Paused→Finish (theo `TConst.CampaignStatus`), danh sách khách + kết quả gọi (Done/Failed/NoAnswer/CallAgain/DoNotCall theo `CampaignCustomerCallStatus`), tiến độ %, controller + 3 view (Index/Details/Create), seed 2 chiến dịch demo, nav "Chiến dịch". Build Release 0 error; verify render Index/Details/Create + chuyển trạng thái Pending→Approved qua browser (0 console error). Commit 65c84dc.
- 2026-09-23 · feat: Chính sách SLA — port từ SkyCS `Mst_SLA` + logic tính vi phạm trong `ETicket.cs` (11.BackEnd/V10/idn.SkyCS.Biz). Thêm entity `SlaPolicy` (Code/Level/FirstResMinutes/ResolutionMinutes/IsActive) + `Ticket.SlaPolicyId` và các thuộc tính toán `ActualFirstResMinutes`/`ActualResolutionMinutes`/`ViolatesFirstResponse`/`ViolatesResolution` (theo `ET_TicketCalcProcessTimeX`: vi phạm khi thời gian thực tế > cam kết). Controller `SlaController` + 2 view (Index có thẻ thống kê vi phạm, Edit), gắn SLA vào form tạo phiếu, panel "Cam kết SLA" trên chi tiết phiếu, nav "Chính sách SLA", seed 3 mức SLAVIP/Tiêu chuẩn/Thấp) + gán SLA cho phiếu demo. Build Release 0 error; verify /Sla (200, hiện SLA-VIP + thống kê vi phạm), /Sla/Edit (200), /Ticket/Details (200, panel SLA + cảnh báo vi phạm). Commit 8ccc20d.
- 2026-09-23 · feat: Đánh giá phiếu (eTicket Rating) — port từ SkyCS `ET_TicketRated` + `ET_Ticket.FlagRated` (11.BackEnd/V10/idn.SkyCS.Biz/ETicket.cs, `ET_TicketRated_SaveX`/`ET_TicketRated_ReviewX`; controller `ETTicketRatedController.cs`). Thêm entity `TicketRating` (FormCode=HstIdx, RateRound=HstRate, RateType RATE/REVIEW, Status=RATESTATUS, Result=RATERESULT, Score 1–5 sao, Comment, RatedBy/ReviewedBy) + enum `RateType`/`RateStatus`/`RateResult` + cờ `Ticket.FlagRated` (chỉ đánh giá khi cờ chưa bật, theo check `FlagRated='1'`). Service `RateTicketAsync`/`ReviewRatingAsync`/`RatingsAsync`/`RatingStatsAsync`, controller `RatingController` + 3 view (Index có thẻ thống kê điểm TB/hài lòng, Rate form khách, Review form agent), panel "Đánh giá phiếu" trên chi tiết phiếu, nav "Đánh giá phiếu", seed 2 đánh giá demo. Build Release 0 error; verify /Rating (200, 2 dòng + thống kê), /Rating/Rate (200, form 3 mức + 5 sao), /Rating/Review (200), /Ticket/Details (200, panel đánh giá) và POST đánh giá phiếu → redirect Details hiện "Đã đánh giá" + nhận xét (0 console error, 0 request lỗi).
- 2026-09-23 · feat: Mẫu khảo sát hài lòng (SurveyForm) — port từ SkyCS `St_SurveyForm` + `St_SurveyFormDetail` (12.Dev.Common/idn.SkyCS.Common/Models; SQL `20250326.z11.CreTbl.St_SurveyForm.sql`; controller `StSurveyFormController.cs`). Thêm entity `SurveyForm` (Code=FrmSurveyCode, Name=FrmSurveyName, Description=FrmSurveyDesc, Remark, IsActive=FlagActive, UsedAt=DTimeUsed, CreatedBy/CreatedAt/UpdatedAt) + `SurveyFormField` (Code=CampaignColCfgCodeSys, Name=CampaignColCfgName, FieldType=CampaignColCfgDataType, Order=Idx, Width=ColWidth, IsRequired=FlagRequired, Options=JsonListOption) + enum `SurveyFieldType` (Text/Number/Rating/SingleChoice/MultiChoice/Date). Service `SurveyFormsAsync`/`SurveyFormGetAsync`/`SurveyFormSaveAsync`/`SurveyFormToggleAsync`/`SurveyStatsAsync`, controller `SurveyController` + 3 view (Index có thẻ thống kê mẫu/trường, Details xem trường, Edit/Create form động thêm/xóa trường), nav "Mẫu khảo sát", seed 2 mẫu demo (SAT-STD 4 trường, SAT-VIP 4 trường). Build Release 0 error; verify /Survey (200, 2 mẫu + thống kê 8 trường), /Survey/Details/1 (200, 4 trường), /Survey/Edit/1 (200, 4 dòng trường), /Survey/Create (200) và POST tạo mẫu mới → redirect Details hiện "Đã lưu mẫu khảo sát" (0 console error, 0 request lỗi).
- 2026-09-23 · feat: Cải tiến chất lượng dịch vụ (Service Improvement) — port từ SkyCS `SvImp_SvImprv` + các bảng con `SvImp_SvImprvHonorific/DenyWord/CallTalkTime/Audio` + `Mst_SvImprvItemType` (11.BackEnd/V10/idn.SkyCS.Biz/ServiceImprovement.cs, `SvImp_SvImprv_Get`/`SvImp_SvImprv_Save`; models 12.Dev.Common/idn.SkyCS.Common/Models). Thêm entity `ServiceImprovement` (Code=SvImprvCode, Name=SvImprvName, ItemType=SvImprvItType, Remark, IsActive=FlagActive, UsedAt=DtimeUsed, CreatedBy/CreatedAt/UpdatedAt) + `SvImprvCriterion` (gộp 4 bảng con theo `Kind`: Word=WordDesc, QtyStd, IsRequired=FlagIsRequire, Min/Max=TalkTimeMin/MaxValue, QtyAllow) + enum `SvImprvItemType` (Honorific/DenyWord/CallTalkTime/Audio). Service `SvImprvsAsync`/`SvImprvGetAsync`/`SvImprvSaveAsync`/`SvImprvToggleAsync`/`SvImprvStatsAsync`, controller `SvImprvController` + 3 view (Index có thẻ thống kê bộ/tiêu chí, Details xem tiêu chí, Edit/Create form động thêm/xóa tiêu chí), nav "Cải tiến chất lượng", seed 2 bộ demo (SI-CALL 4 tiêu chí, SI-AUDIO 2 tiêu chí). Build Release 0 error; verify /SvImprv (200, 2 bộ + thống kê 6 tiêu chí), /SvImprv/Details/1 (200, 4 tiêu chí), /SvImprv/Edit/1 (200, 4 dòng), /SvImprv/Create (200) và POST tạo bộ mới → redirect Details hiện "Đã lưu bộ cải tiến" (0 console error, 0 request lỗi).
- 2026-09-23 · feat: Trung tâm khách hàng (Customer Center) — port từ SkyCS `Mst_Customer` + `Mst_CustomerContact` + `Mst_CustomerHist` + `Mst_CustomerGroup` (11.BackEnd/V10/idn.SkyCS.Biz/CustomerCenter/Customer.cs, `Mst_Customer_SaveX`/`Mst_CustomerContact_SaveX`/`Mst_CustomerHist_SaveX`; Master.cs `Mst_CustomerGroup_SaveX`). Thêm entity `Customer` (Code=CustomerCodeSys, CodeInvoice, Name/NameEN, Type=CustomerType, Partner=PartnerType, TaxCode=MST, GroupCode=CustomerGrpCode, Phone/Email/Address/Province/District, IsActive=FlagActive, UsedAt=DTimeUsed, CreatedBy/CreatedAt/UpdatedAt) + `CustomerContact` (người liên hệ: Name/Title/Phone/Email) + `CustomerHistory` (lịch sử: Action/Detail=JsonCustomerInfoHist/ChangedBy/ChangedAt) + `CustomerGroup` (nhóm KH: Code/Name/Description) + enum `CustomerType` (Cá nhân/Doanh nghiệp) và `PartnerType` (Khách hàng/NCC/Cả hai). Service `CustomersAsync`/`CustomerGetAsync`/`CustomerSaveAsync`/`CustomerToggleAsync`/`CustomerStatsAsync`/`CustomerGroupsAsync`, controller `CustomerController` + 3 view (Index có thẻ thống kê tổng/hoạt động/doanh nghiệp/người liên hệ + lọc theo loại/nhóm/trạng thái, Details xem hồ sơ + người liên hệ + lịch sử thay đổi, Edit/Create form động thêm/xóa người liên hệ), nav "Khách hàng", seed 3 nhóm (VIP/Đại lý/Khách lẻ) + 4 khách hàng demo (2 DN, 1 cá nhân, 1 ngừng). Build Release 0 error. Commit 5005cc3.
- 2026-09-23 · feat: Thiết lập phân bổ phiếu tự động (Allocate Rule) — port từ SkyCS `Mst_EstablishAllocateETicket` + `Mst_EstablishAllocateETAssignAgent` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_EstablishAllocateETicket_Get`/`_SaveX`; schema xác nhận qua migrate SQL `20260311.uat.Migrate.COBToUat.sql`). Thêm entity `AllocateRule` (DepartmentCode, AllocateEven=FlagAllocateEven, AssignAgent=FlagAssignAgent, AllMissedCall=FlagAllMissedCall, IsActive=FlagActive, Remark, CreatedBy/CreatedAt/UpdatedAt) + `AllocateAgent` (AgentCode=Sys_User.UserCode, Remark, IsActive) — cấu hình cách hệ thống tự phân eTicket mới về phòng ban và gán agent (chia đều / theo thứ tự / chỉ về phòng ban). Service `AllocateRulesAsync`/`AllocateRuleGetAsync`/`AllocateRuleSaveAsync`/`AllocateRuleToggleAsync`/`AllocateStatsAsync`, controller `AllocateController` + 3 view (Index có thẻ thống kê tổng/đang dùng/tự gán agent/agent nhận phiếu + lọc trạng thái, Details xem cấu hình + danh sách agent, Edit/Create form động thêm/xóa agent), nav "Phân bổ phiếu", seed 3 thiết lập demo (PB-KYTHUAT chia đều 3 agent, PB-CSKH theo thứ tự 2 agent, PB-TONGDAI ngừng dùng). Build Release 0 error. Commit 925a8e1 (đã push origin/main).
- 2026-09-23 · feat: Thiết lập nhắc nhở phiếu (Reminder Rule) — port từ SkyCS `Mst_EstablishRemindETicket` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_EstablishRemindETicket_Get`/`_SaveX`). Thêm entity `ReminderRule` (EstablishId=EstablishID, NotifySystem=FlagNotifySystem, NotifyEmail=FlagNotifyEmail, NotifySms=FlagNotifySMS, NotifyZalo=FlagNotifyZalo, SubFormCodeEmail/Sms/Zalo, IsActive=FlagActive, Remark, CreatedBy/CreatedAt/UpdatedAt) + enum `RemindChannel` (System/Email/Sms/Zalo) — cấu hình kênh thông báo khi phiếu tới hạn/quá hạn xử lý, mỗi kênh gắn mẫu nội dung riêng. Service `ReminderRulesAsync`/`ReminderRuleGetAsync`/`ReminderRuleSaveAsync`/`ReminderRuleToggleAsync`/`ReminderStatsAsync`, controller `ReminderController` + 3 view (Index có thẻ thống kê tổng/đang dùng/kênh Email/kênh SMS-Zalo + lọc trạng thái & kênh, Details xem 4 kênh + mẫu nội dung, Edit/Create form bật/tắt kênh + nhập mẫu), nav "Nhắc nhở phiếu", seed 3 thiết lập demo (RM-SLA hệ thống+email+zalo, RM-OVERDUE hệ thống+email+sms, RM-OLD ngừng dùng). Build Release 0 error. Commit 8a907e9.
- 2026-09-23 · feat: Danh mục phiếu (Ticket Catalog) — port từ SkyCS `Mst_TicketStatus` + `Mst_TicketPriority` + `Mst_TicketSource` + `Mst_ReceptionChannel` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_TicketStatus_Save`/`_Get`, `Mst_TicketPriority_Save`, `Mst_TicketSource_Save`, `Mst_ReceptionChannel_Save`; seed thật `20231122.ScriptSkyCS.sql`). Gộp 4 bảng danh mục dùng chung cho eTicket vào 1 entity `TicketCatalog` (Kind=Status/Priority/Source/ReceptionChannel, Code, AgentName, CustomerName, UseType=FlagUseType TYPE1/2/3, IsActive=FlagActive, Remark, CreatedBy/CreatedAt/UpdatedAt) + enum `TicketCatalogKind`/`CatalogUseType`. Service `TicketCatalogsAsync`/`TicketCatalogGetAsync`/`TicketCatalogSaveAsync`/`TicketCatalogToggleAsync`/`TicketCatalogStatsAsync`, controller `TicketCatalogController` + 3 view (Index có thẻ thống kê tổng/đang dùng/trạng thái-ưu tiên/nguồn-kênh + lọc theo loại & trạng thái, Details xem tên hiển thị agent/khách, Edit/Create form chọn loại + phạm vi sử dụng), nav "Danh mục phiếu", seed 19 dòng demo theo giá trị thật SkyCS (7 trạng thái, 4 ưu tiên, 3 nguồn, 5 kênh tiếp nhận). Build Release 0 error. Commit 4c9db48 (đã push origin/main).
- 2026-09-23 · feat: Phòng ban (Department) — port từ SkyCS `Mst_Department` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_Department_Get`/`_Update`; model 12.Dev.Common/idn.SkyCS.Common/Models/Mst_Department.cs; schema xác nhận qua migrate SQL `20231122.ScriptSkyCS.sql`). Thêm entity `Department` (Code=DepartmentCode, ParentCode=DepartmentCodeParent, Name=DepartmentName, Description=DepartmentDesc, Level=DepartmentLevel, TaxCode=MST, AutoDiv=FlagAutoDiv, IsActive=FlagActive, Order=OrderIdx, CreatedBy/CreatedAt/UpdatedAt) + `DepartmentMember` (thành viên phòng ban = Sys_UserMapDepartment: UserCode, FullName, Email=Email, Phone=PhoneNo, IsActive) — phòng ban là đơn vị tổ chức nhận/xử lý eTicket, có phân cấp (cấp trên) và cờ chia đều tự động cho thành viên; là master data nền cho phân bổ phiếu (AllocateRule.DepartmentCode). Service `DepartmentsAsync`/`DepartmentGetAsync`/`DepartmentSaveAsync`/`DepartmentToggleAsync`/`DepartmentStatsAsync`, controller `DepartmentController` + 3 view (Index có thẻ thống kê tổng/đang dùng/cấp 1-chia đều/thành viên + lọc trạng thái, Details xem thông tin + danh sách thành viên, Edit/Create form động thêm/xóa thành viên), nav "Phòng ban", seed 4 phòng ban demo (PB-CSKH gốc chia đều 2 TV, PB-KYTHUAT cấp 2, PB-TONGDAI cấp 2, PB-CU ngừng dùng). Build Release 0 error. Commit d88780b (đã push origin/main).
- 2026-09-23 · feat: Điều khoản thanh toán (Payment Term) — port từ SkyCS `Mst_PaymentTerm` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_PaymentTerm_Get`/`_SaveX`; model 12.Dev.Common/idn.SkyCS.Common/Models/Mst_PaymentTerm.cs; hằng số `TConst.PTType` = SALE/PURCHASE; cột xác nhận qua `TblMst_PaymentTerm` trong Const.Main.cs). Thêm entity `PaymentTerm` (Code=PaymentTermCode, Name=PaymentTermName, Type=PTType, Description=PTDesc, OwedDay=số ngày được nợ, CreditLimit=hạn mức công nợ, DepositPercent=% đặt cọc, IsActive=FlagActive, Remark, CreatedBy/CreatedAt/UpdatedAt) + enum `PTType` (Bán ra/Mua vào) — quy định cách khách hàng thanh toán, là master data nền cho hồ sơ khách hàng/đơn hàng. Service `PaymentTermsAsync`/`PaymentTermGetAsync`/`PaymentTermSaveAsync`/`PaymentTermToggleAsync`/`PaymentTermStatsAsync`, controller `PaymentTermController` + 3 view (Index có thẻ thống kê tổng/đang dùng/bán-mua/có hạn mức nợ + lọc theo loại & trạng thái, Details xem loại/số ngày nợ/hạn mức/% cọc, Edit/Create form nhập đầy đủ), nav "Điều khoản thanh toán", seed 5 điều khoản demo (PT-NGAY, PT-30, PT-DL cọc 30%, PT-MUA nợ 45 ngày, PT-CU ngừng dùng). Kiểm tra theo `Mst_PaymentTerm_SaveX`: mã/tên/mô tả bắt buộc, OwedDay/DepositPercent/CreditLimit không âm. Build Release 0 error. Commit 3ad662f (đã push origin/main).- 2026-09-23 · feat: Vùng thị trường (Area) — port từ SkyCS `Mst_Area` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_Area_Get`/`_SaveX`; model 12.Dev.Common/idn.SkyCS.Common/Models/CustomerCentrer/Mst_Area.cs; schema xác nhận qua migrate SQL `20231122.ScriptCustomerCenter.sql`/`20231122.ScriptSkyCS.sql`). Thêm entity `Area` (Code=AreaCode, ParentCode=AreaCodeParent, Name=AreaName, Description=AreaDesc, Level=AreaLevel, BUCode=AreaBUCode, BUPattern=AreaBUPattern, IsActive=FlagActive, CreatedBy/CreatedAt/UpdatedAt) — master data Trung tâm khách hàng phân vùng khách hàng theo khu vực, có phân cấp (vùng gốc → vùng con) và cấp vùng; là nền cho phân vùng phục vụ/báo cáo theo khu vực (SkyCS gắn khách vào vùng qua `Mst_CustomerInArea`). Service `AreasAsync`/`AreaGetAsync`/`AreaSaveAsync`/`AreaToggleAsync`/`AreaStatsAsync`, controller `AreaController` + 3 view (Index có thẻ thống kê tổng/đang dùng/vùng gốc-vùng con/cấp sâu nhất + lọc trạng thái, Details xem cấp/vùng cấp trên/đơn vị KD, Edit/Create form nhập đầy đủ), nav "Vùng thị trường", seed 5 vùng demo (MB, MN cấp 1; MB-HN, MN-HCM cấp 2; MN-CT ngừng dùng). Kiểm tra theo `Mst_Area_SaveX`: mã vùng (AreaCode) bắt buộc, cấp vùng ≥ 1. Build Release 0 error. Commit 25a9b80 (đã push origin/main).
- 2026-09-23 · feat: Thẻ (Tag) — port từ SkyCS `Mst_Tag` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_Tag_Get`/`Mst_Tag_Create`/`Mst_Tag_CheckDB`; model 12.Dev.Common/idn.SkyCS.Common/Models/Mst_Tag.cs; controller 13.ClientGate/V20/idn.SkyCS.WebAPI/Controllers/MstTagController.cs; cột xác nhận qua `TblMst_Tag` trong Const.Main.cs). Thêm entity `Tag` (Code=TagID, Name=TagName, Description=TagDesc, Slug, IsActive=FlagActive, CreatedBy=LogLUBy/CreatedAt=LogLUDTimeUTC/UpdatedAt) + helper `SlugText`/`MakeSlug` (sinh slug bỏ dấu tiếng Việt) — thẻ là nhãn dùng chung để gắn/phân loại nội dung CSKH (bài viết Knowledge Base, phiếu hỗ trợ). Service `TagsAsync`/`TagGetAsync`/`TagSaveAsync`/`TagToggleAsync`/`TagStatsAsync`, controller `TagController` + 3 view (Index có thẻ thống kê tổng/đang dùng/có slug/ngừng dùng + lọc trạng thái & tìm kiếm, Details xem mã/slug/người tạo, Edit/Create form nhập mã/tên/slug/mô tả), nav "Thẻ", seed 5 thẻ demo (TAG-KHIEUNAI, TAG-HOADON, TAG-BAOHANH, TAG-VIP, TAG-CU ngừng dùng). Kiểm tra theo `Mst_Tag_Create`: TagName bắt buộc, để trống mã/slug sẽ tự sinh. Build Release 0 error. Commit b00309f (đã push origin/main).- 2026-09-23 · feat: Người nhận thông báo phiếu (Receive Notify) — port từ SkyCS `Mst_EstablishReceiveNotifyETicket` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_EstablishReceiveNotifyETicket_Get`/`_SaveX`; model 12.Dev.Common/idn.SkyCS.Common/Models/Mst_EstablishReceiveNotifyETicket.cs; controller 13.ClientGate/V20/idn.SkyCS.WebAPI/Controllers/MstEstablishReceiveNotifyETicketController.cs). Thêm entity `ReceiveNotify` (AgentCode=Sys_User.UserCode, AgentName=su_UserName, Remark, CreatedBy=LogLUBy/CreatedAt=LogLUDTimeUTC/UpdatedAt) — danh sách agent sẽ nhận thông báo khi có eTicket mới (cấu hình dạng danh sách theo tổ chức, theo `_SaveX` là ClearAll/InsertAll). Service `ReceiveNotifiesAsync`/`ReceiveNotifyGetAsync`/`ReceiveNotifySaveAsync`/`ReceiveNotifyDeleteAsync`/`ReceiveNotifyStatsAsync`, controller `ReceiveNotifyController` + 3 view (Index có thẻ thống kê tổng/có tên/có ghi chú/agent khác nhau + tìm kiếm, Details xem mã-tên agent + người tạo, Edit/Create form nhập mã/tên/ghi chú), nav "Người nhận thông báo", seed 3 người nhận demo (ha.nguyen, minh.tran, lan.le). Kiểm tra theo `Mst_EstablishReceiveNotifyETicket_SaveX`: AgentCode bắt buộc. Build Release 0 error (0 warning). Commit d5b280b (đã push origin/main).
- 2026-09-23 · feat: Danh mục địa chỉ (Province/District/Ward) — port từ SkyCS `Mst_Province` + `Mst_District` + `Mst_Ward` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_Province_Get`/`Mst_District_Get`/`Mst_Ward_Get`; models 12.Dev.Common/idn.SkyCS.Common/Models/Mst_Province.cs, Mst_District.cs, CustomerCentrer/Mst_Ward.cs). Gộp 3 bảng địa chỉ hành chính vào 1 entity `Address` (Level=Province/District/Ward, Code=ProvinceCode/DistrictCode/WardCode, Name=*Name, ParentCode=ProvinceCode(District)/DistrictCode(Ward), PostCode, CountryCode, IsActive=FlagActive, CreatedBy=LogLUBy/CreatedAt=LogLUDTimeUTC/UpdatedAt) + enum `AddressLevel` — master data nền cho địa chỉ khách hàng (Mst_Customer.ProvinceCode/DistrictCode). Service `AddressesAsync`/`AddressGetAsync`/`AddressSaveAsync`/`AddressToggleAsync`/`AddressStatsAsync`, controller `AddressController` + 3 view (Index có thẻ thống kê tổng/đang dùng/tỉnh-quận-phường + lọc theo cấp/trạng thái/cấp trên & tìm kiếm, Details xem cấp/mã/cấp trên/mã bưu chính, Edit/Create form chọn cấp + nhập mã/tên/cấp trên), nav "Danh mục địa chỉ", seed 12 dòng demo (4 tỉnh, 4 quận, 4 phường; có 1 tỉnh + 1 phường ngừng dùng). Kiểm tra theo `Mst_Province/District/Ward`: mã + tên bắt buộc, Quận/Huyện và Phường/Xã phải có cấp trên. Build Release 0 error. Commit 69b0f3e (đã push origin/main).
- 2026-09-24 · feat: Lịch làm việc SLA (SLA Working Calendar) — port từ SkyCS `Mst_SLAWorkingDay` + `Mst_SLAHoliday` (11.BackEnd/V10/idn.SkyCS.Biz/Master.1.cs, `Mst_SLA_Get`/`Mst_SLA_SaveX`/`Mst_SLA_CalcDeadlineX`/`Mst_SLA_CalcFirstResDTimeX`; models 12.Dev.Common/idn.SkyCS.Common/Models/Mst_SLAWorkingDay.cs, Mst_SLAHoliday.cs; controller 13.ClientGate/V20/idn.SkyCS.WebAPI/Controllers/MstSLAController.cs). Thêm entity `SlaWorkingDay` (SlaPolicyId=SLAID, WeekdayCode=SLAWorkingDayCode 1=CN…7=Thứ bảy, Shift=Idx 1 sáng/2 chiều, FromMinutes/ToMinutes=WorkingDTimeFrom/To tính bằng phút từ 00:00) + `SlaHoliday` (Holiday=SLAHoliday dạng "dd-MM" lặp hằng năm, Name=SLAHolidayName) + enum `SlaShift` — mỗi chính sách SLA gắn một lịch làm việc (giờ theo từng thứ, 2 ca/ngày) + danh sách ngày nghỉ, dùng để tính hạn xử lý theo giờ hành chính thay vì 24/7. Service `SlaCalendarsAsync`/`SlaCalendarGetAsync`/`SlaWorkingDaysAsync`/`SlaHolidaysAsync`/`SlaCalendarSaveAsync` (thay-thế toàn bộ lịch + ngày nghỉ, theo `_SaveX` clear-all/insert-all)/`SlaCalendarStatsAsync`/`SlaCalcDeadlineAsync` (port thuật toán `Mst_SLA_CalcFirstResDTimeX`: cộng dồn số phút cam kết, chỉ tính trong ca làm việc, bỏ qua ngày nghỉ và ngày không có ca; không có lịch → cộng thẳng 24/7), controller `SlaCalendarController` + 3 view (Index có thẻ thống kê chính sách/có lịch/có ngày nghỉ/số ca-ngày nghỉ + tìm kiếm, Details xem giờ theo thứ + ngày nghỉ + form "Tính thử hạn xử lý", Edit form động thêm/xóa ca và ngày nghỉ), nav "Lịch làm việc SLA", seed lịch T2–T6 2 ca cho SLA-VIP (08:00–12:00 & 13:00–17:00) và SLA-STD (08:30–12:00 & 13:30–17:30) + 5 ngày nghỉ demo (01-01, 30-04, 01-05, 02-09). Build Release 0 error, 0 warning. Commit 2aae4fa (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniCSKH, không sửa nguồn.- 2026-09-24 · feat: Kênh liên hệ (Contact Channel) — port từ SkyCS `Mst_ContactChannel` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_ContactChannel_Get`/`_Save`; model 12.Dev.Common/idn.SkyCS.Common/Models/Mst_ContactChannel.cs; dùng trong `ETTicketController` để xác định cách liên hệ khách theo tên agent). Thêm entity `ContactChannel` (Code=ContactChannel, AgentName=AgentContactChannelName, CustomerName=CustomerContactChannelName, UseType=FlagUseType TYPE1/2/3, IsActive=FlagActive, Remark, CreatedBy=LogLUBy/CreatedAt=LogLUDTimeUTC/UpdatedAt) — master data quy định CÁCH liên hệ với khách hàng (gọi điện, email, Zalo, SMS, gặp trực tiếp…), khác với "kênh tiếp nhận" (Mst_ReceptionChannel — nơi phiếu được tạo đến, đã port ở Danh mục phiếu). Service `ContactChannelsAsync`/`ContactChannelGetAsync`/`ContactChannelSaveAsync` (validate theo `Mst_ContactChannel_Save`: tên cho agent + tên cho khách bắt buộc)/`ContactChannelToggleAsync`/`ContactChannelStatsAsync`, controller `ContactChannelController` + 3 view (Index có thẻ thống kê tổng/đang dùng/chỉ agent/agent-khách + lọc theo phạm vi & trạng thái, Details xem mã/phạm vi/tên hiển thị agent-khách, Edit/Create form nhập mã/tên/phạm vi/trạng thái), nav "Kênh liên hệ", seed 6 kênh demo (CALL, EMAIL, ZALO, SMS, MEET, FAX ngừng dùng). Build Release 0 error, 0 warning. Commit 71b340f (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniCSKH, không sửa nguồn.- 2026-09-24 · feat: Loại phiếu tùy chỉnh (Ticket Custom Type) — port từ SkyCS `Mst_TicketCustomType` + `Mst_TicketTypeMapCustom` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_TicketCustomType_Get`/`Mst_TicketCustomType_GetByTicketType`/`Mst_TicketCustomType_Save`; models 12.Dev.Common/idn.SkyCS.Common/Models/Mst_TicketCustomType.cs, Mst_TicketTypeMapCustom.cs; cột xác nhận qua `TblMst_TicketCustomType` trong Const.Main.cs; dùng trong `ETicket.cs` để join tên agent/khách theo `ET_Ticket.TicketCustomType`). Thêm entity `TicketCustomType` (Code=TicketCustomType, AgentName=AgentTicketCustomTypeName, CustomerName=CustomerTicketCustomTypeName, UseType=FlagUseType TYPE1/2/3, IsActive=FlagActive, Remark, CreatedBy=LogLUBy/CreatedAt=LogLUDTimeUTC/UpdatedAt) + `TicketCustomTypeMap` (gán loại tùy chỉnh cho phân loại nghiệp vụ: TicketTypeCode=TicketType, Remark, IsActive) — phân loại con của eTicket, mỗi loại có tên riêng cho agent/khách và được gán cho từng `Mst_TicketType` qua bảng map (theo `Mst_TicketTypeMapCustom`). Service `TicketCustomTypesAsync`/`TicketCustomTypeGetAsync`/`TicketCustomTypeSaveAsync` (validate theo `Mst_TicketCustomType_Save`: tên cho agent + tên cho khách bắt buộc, FlagUseType phải TYPE1/2/3; thay-thế toàn bộ danh sách map)/`TicketCustomTypeToggleAsync`/`TicketCustomTypeStatsAsync`, controller `TicketCustomTypeController` + 3 view (Index có thẻ thống kê tổng/đang dùng/agent-khách/lượt gán phân loại + lọc theo phạm vi & trạng thái, Details xem mã/phạm vi/tên hiển thị + danh sách phân loại áp dụng, Edit/Create form động thêm/xóa phân loại), nav "Loại phiếu tùy chỉnh", seed 4 loại demo (TCT-KYTHUAT gán TT-SUPPORT, TCT-HOADON gán TT-SUPPORT+TT-COMPLAINT, TCT-KHAOSAT gán TT-SURVEY, TCT-CU ngừng dùng). Build Release 0 error, 0 warning. Commit 103652d (đã push origin/main). Chỉ sửa file trong MiniCSKH, không sửa nguồn.- 2026-09-24 · feat: Điều kiện áp dụng SLA (SLA Assignment Scope) — port từ SkyCS `Mst_SLATicketType` + `Mst_SLATicketCustomType` + `Mst_SLACustomerCN` + `Mst_SLACustomerGroupCN` + `Mst_SLACustomerDN` + `Mst_SLACustomerGroupDN` và các cờ `FlagAll*` trên `Mst_SLA` (11.BackEnd/V10/idn.SkyCS.Biz/Master.1.cs, `Mst_SLA_Get`/`Mst_SLA_SaveX`; models 12.Dev.Common/idn.SkyCS.Common/Models/Mst_SLATicketType.cs, Mst_SLACustomerCN.cs, Mst_SLACustomerGroupCN.cs, Mst_SLACustomerDN.cs, Mst_SLACustomerGroupDN.cs, Mst_SLA.cs). Thêm entity `SlaScope` (gộp 6 bảng con theo `Kind`: TicketType/TicketCustomType/CustomerCN/CustomerGroupCN/CustomerDN/CustomerGroupDN, RefCode = TicketType/TicketCustomType/CustomerCodeSys/CustomerGrpCode) + enum `SlaScopeKind` + 6 cờ "áp dụng cho tất cả" trên `SlaPolicy` (AllTicketType/AllTicketCustomType/AllCustomerCN/AllCustomerGroupCN/AllCustomerDN/AllCustomerGroupDN = FlagAll*) — quy định chính sách SLA nào áp dụng cho loại phiếu / khách hàng / nhóm khách nào; khi tạo eTicket hệ thống dò các điều kiện này để chọn đúng SLA (theo `Mst_SLA_Get` join 6 bảng con + so khớp cờ FlagAll). Service `SlaScopesAsync`/`SlaScopeGetAsync`/`SlaScopeSaveAsync` (thay-thế toàn bộ danh sách đối tượng, theo `_SaveX` clear-all/insert-all)/`SlaScopeStatsAsync`, controller `SlaScopeController` + 3 view (Index có thẻ thống kê chính sách/có đối tượng/bật "tất cả"/dòng đối tượng + tìm kiếm, Details xem 6 cờ "tất cả" + bảng đối tượng cụ thể, Edit form bật/tắt cờ + động thêm/xóa đối tượng chọn loại + nhập mã), nav "Điều kiện áp dụng SLA", seed demo (SLA-VIP: cờ nhóm khách cá nhân + loại phiếu TT-COMPLAINT + nhóm GRP-VIP; SLA-STD: cờ mọi loại phiếu; SLA-LOW: loại phiếu TT-SUPPORT + khách DN KH0002). Build Release 0 error, 0 warning. Commit fad660c (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniCSKH, không sửa nguồn.- 2026-09-24 · feat: Người nộp thuế (Taxpayer) — port từ SkyCS `Mst_NNT` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_NNT_Get`/`Mst_NNT_Update`/`Mst_NNT_CreateForNetwork`; model 12.Dev.Common/idn.SkyCS.Common/Models/Mst_NNT.cs; controller 13.ClientGate/V20/idn.SkyCS.WebAPI/Controllers/MstNNTController.cs; hằng số `MstNNT_TCTStatus` trong Const.Main.BE.cs). Thêm entity `Taxpayer` (TaxCode=MST, FullName=NNTFullName, ShortName, ParentTaxCode=MSTParent, Level=MSTLevel, BUCode/BUPattern, Address=NNTAddress, ProvinceCode/DistrictCode, Mobile/Phone/Fax/Website, PresentBy/Position/BusinessRegNo/PresentIDNo/PresentIDType/GovTaxID, ContactName/ContactPhone/ContactEmail, CANumber/CAOrg/CAEffStart/CAEffEnd, AccNo/AccHolder/BankName, BizType/BizFieldCode/BizSizeCode/AreaCode, TctStatus=TCTStatus, IsActive=FlagActive, Remark, CreatedBy/CreatedAt/UpdatedAt) + enum `TctStatus` (None/Registered/Cancelled theo `MstNNT_TCTStatus`) — hồ sơ doanh nghiệp/tổ chức nộp thuế trong Trung tâm khách hàng, là master data nền cho hồ sơ khách hàng doanh nghiệp và đăng ký dịch vụ TVAN. Service `TaxpayersAsync`/`TaxpayerGetAsync`/`TaxpayerSaveAsync`/`TaxpayerToggleAsync`/`TaxpayerStatsAsync` (kiểm tra theo `Mst_NNT_Update`: MST, tên doanh nghiệp, địa chỉ, người liên hệ tên/ĐT/email bắt buộc), controller `TaxpayerController` + 3 view (Index có thẻ thống kê tổng/đang dùng/TCT xác nhận ĐK/có người liên hệ + lọc theo trạng thái TCT & trạng thái + tìm kiếm, Details xem thông tin doanh nghiệp/pháp lý/chữ ký số-ngân hàng/người liên hệ/trạng thái, Edit/Create form nhập đầy đủ), nav "Người nộp thuế", seed 3 hồ sơ demo (Minh Anh đã ĐK TVAN, Phương Nam chưa ĐK, ABC ngừng dùng). Build Release 0 error, 0 warning. Commit ab7b87a (đã push origin/main). Chỉ sửa file trong MiniCSKH, không sửa nguồn.- 2026-09-24 · feat: Loại chiến dịch (Campaign Type) — port từ SkyCS `Mst_CampaignType` + `Mst_CustomColumnCampaignType` + `Mst_CustomerFeedBack` (11.BackEnd/V10/idn.SkyCS.Biz/Campaign.cs, `Mst_CampaignType_Get`/`Mst_CampaignType_SaveX`; models 12.Dev.Common/idn.SkyCS.Common/Models/Mst_CampaignType.cs, Mst_CustomColumnCampaignType.cs, Mst_CustomerFeedBack.cs). Thêm entity `CampaignType` (Code=CampaignTypeCode, Name=CampaignTypeName, Description=CampaignTypeDesc, Remark, IsActive=FlagActive, CreatedBy=LogLUBy/CreatedAt=LogLUDTimeUTC/UpdatedAt) + `CampaignTypeColumn` (trường tùy chỉnh = Mst_CustomColumnCampaignType: Code=CampaignColCfgCodeSys, Name=CampaignColCfgName, FieldType=CampaignColCfgDataType, Order=Idx, IsRequired=FlagRequired) + `CampaignFeedback` (phản hồi KH = Mst_CustomerFeedBack: Code=CusFBCode, Name=CusFBName) — loại chiến dịch phân loại chiến dịch gọi ra (Cpn_Campaign.CampaignTypeCode), gắn bố cục trường nhập liệu và danh sách phản hồi chuẩn. Service `CampaignTypesAsync`/`CampaignTypeGetAsync`/`CampaignTypeSaveAsync` (thay-thế toàn bộ trường + phản hồi, theo `_SaveX` clear-all/insert-all)/`CampaignTypeToggleAsync`/`CampaignTypeStatsAsync`/`CampaignTypeUsageAsync` (đếm chiến dịch đang dùng loại — theo check chặn xóa trong `Mst_CampaignType_SaveX`), controller `CampaignTypeController` + 3 view (Index có thẻ thống kê tổng/đang dùng/trường tùy chỉnh/phản hồi + lọc trạng thái & tìm kiếm, Details xem trường + phản hồi + cảnh báo "đang được N chiến dịch dùng", Edit/Create form động thêm/xóa trường và phản hồi), nav "Loại chiến dịch", seed 3 loại demo (TELESALES 3 trường + 3 phản hồi, SURVEY 2 trường + 3 phản hồi, CT-OLD ngừng dùng). Kiểm tra theo `Mst_CampaignType_SaveX`: mã + tên bắt buộc. Build Release 0 error, 0 warning. Commit 428b2a8 (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniCSKH, không sửa nguồn.- 2026-09-24 · feat: Loại giấy tờ định danh (GovIDType) — port từ SkyCS `Mst_GovIDType` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_GovIDType_Get`; model 12.Dev.Common/idn.SkyCS.Common/Models/Mst_GovIDType.cs; controller 13.ClientGate/V20/idn.SkyCS.WebAPI/Controllers/MstGovIDTypeController.cs; cột xác nhận qua `TblMst_GovIDType` trong Const.Main.cs; seed thật `20200416.Init.Data.sql`). Thêm entity `GovIDType` (Code=GovIDType, Name=GovIDTypeName, Remark, IsActive=FlagActive, CreatedBy=LogLUBy/CreatedAt=LogLUDTimeUTC/UpdatedAt) — master data quy định các loại giấy tờ tùy thân/định danh (CMTND/Thẻ căn cước, Hộ chiếu, Bằng lái xe, Giấy tờ khác…) dùng khi ghi nhận thông tin khách hàng; hồ sơ khách hàng/người nộp thuế tham chiếu qua PresentIDType. Service `GovIDTypesAsync`/`GovIDTypeGetAsync`/`GovIDTypeSaveAsync`/`GovIDTypeToggleAsync`/`GovIDTypeStatsAsync`, controller `GovIDTypeController` + 3 view (Index có thẻ thống kê tổng/đang dùng/ngừng dùng/có ghi chú + lọc trạng thái & tìm kiếm, Details xem mã/tên/trạng thái, Edit/Create form nhập mã/tên/ghi chú), nav "Loại giấy tờ định danh", seed 5 dòng demo theo giá trị thật SkyCS (CMTND_THECANCUOC, HOCHIEU, BANGLAIXE, ORTHER + 1 loại ngừng dùng). Kiểm tra theo `Mst_GovIDType`: tên loại giấy tờ bắt buộc, để trống mã sẽ tự sinh. Build Release 0 error, 0 warning. Commit 93c2d68 (đã push origin/main). Chỉ sửa file trong MiniCSKH, không sửa nguồn.
- 2026-09-24 · feat: Mức đánh giá hài lòng (Satisfaction Rating) — port từ SkyCS `Mst_SatisfactionRating` (11.BackEnd/V10/idn.SkyCS.Biz/Master.cs, `Mst_SatisfactionRating_Get`/`Mst_SatisfactionRating_CheckDB`; model 12.Dev.Common/idn.SkyCS.Common/Models/Mst_SatisfactionRating.cs; controller 13.ClientGate/V20/idn.SkyCS.WebAPI/Controllers/MstSatisfactionRatingController.cs). Thêm entity `SatisfactionRating` (Code=SatRatingCode, Name=SatRatingName, Order=OrdIdx, Remark, IsActive=FlagActive, CreatedBy=LogLUBy/CreatedAt=LogLUDTimeUTC/UpdatedAt) + thuộc tính `IsPositive` (mức tích cực SAT5/SAT4) — master data quy định các MỨC độ hài lòng chuẩn (Rất hài lòng/Hài lòng/Bình thường/Không hài lòng/Rất không hài lòng) để agent chọn khi ghi nhận kết quả chăm sóc; eTicket gắn mức này qua `ET_Ticket.SatRatingCode` ("Mã Đánh giá mức độ hài lòng"), chiến dịch khảo sát cũng tham chiếu. Service `SatisfactionRatingsAsync`/`SatisfactionRatingGetAsync`/`SatisfactionRatingSaveAsync`/`SatisfactionRatingToggleAsync`/`SatisfactionRatingStatsAsync`, controller `SatisfactionRatingController` + 3 view (Index có thẻ thống kê tổng/đang dùng/ngừng dùng/mức tích cực + lọc trạng thái & tìm kiếm, Details xem mã/tên/thứ tự/mức độ, Edit/Create form nhập mã/tên/thứ tự/ghi chú), nav "Mức đánh giá hài lòng", seed 6 mức demo (SAT5→SAT1 + 1 mức cũ ngừng dùng). Kiểm tra theo `Mst_SatisfactionRating`: tên mức bắt buộc, để trống mã sẽ tự sinh. Build Release 0 error, 0 warning.
- 2026-09-24 · feat: Loại kênh (Channel Type) — port từ SkyCS `Mst_ChannelType` (OmniChannel) (11.BackEnd/V10/idn.SkyCS.Biz/OmniChannel/OminiChannel.cs, `Mst_ChannelType_Get`/`Mst_ChannelType_GetX`; model 12.Dev.Common/idn.SkyCS.Common/Models/OminiChannel/Mst_ChannelType.cs; controller 13.ClientGate/V20/idn.SkyCS.WebAPI/Controllers/MstChannelTypeController.cs; cột xác nhận qua `TblMst_ChannelType` trong Const.Main.1.cs; seed thật `20230619.z11.UpdDB.80.OmniChannel.sql` = EMAIL/SMS/ZALO). Thêm entity `ChannelType` (Code=ChannelType, Name=ChannelTypeName, IsActive=FlagActive, CreatedBy=LogLUBy/CreatedAt=LogLUDTimeUTC/UpdatedAt) + thuộc tính `Icon` (biểu tượng theo mã) — master data của OmniChannel quy định các LOẠI kênh liên lạc đa kênh (email/sms/zalo/call/facebook); `Mst_Channel` gắn loại kênh cho eTicket (ChannelTypeETicket) và OTP (ChannelTypeOTP), các bảng con Mst_ChannelEmail/SMS/Zalo tham chiếu. Service `ChannelTypesAsync`/`ChannelTypeGetAsync`/`ChannelTypeSaveAsync`/`ChannelTypeToggleAsync`/`ChannelTypeStatsAsync`, controller `ChannelTypeController` + 3 view (Index có thẻ thống kê tổng/đang dùng/ngừng dùng/có tên + lọc trạng thái & tìm kiếm, Details xem mã/tên/trạng thái, Edit/Create form nhập mã/tên), nav "Loại kênh", seed 6 loại demo (EMAIL/SMS/ZALO/CALL/FACEBOOK + 1 loại cũ ngừng dùng). Kiểm tra theo `Mst_ChannelType`: tên loại kênh bắt buộc, để trống mã sẽ tự sinh. Build Release 0 error, 0 warning. Commit c93997e (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniCSKH, không sửa nguồn.
